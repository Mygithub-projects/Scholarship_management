services:
  scholarship:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: scholarship-management

    ports:
      - "3333:3333"

    env_file:
      - .env.docker

    restart: unless-stopped
